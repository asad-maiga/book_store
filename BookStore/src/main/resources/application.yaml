spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: your_postgres_name
    password:  your_postgres_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true

